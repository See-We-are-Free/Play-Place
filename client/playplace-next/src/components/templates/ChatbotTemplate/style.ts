import styled from 'styled-components';

const ChatbotTemplateContainer = styled.div`
	position: relative;
	width: 100%;
	display: flex;
	flex-direction: column;

	#bottom-menu {
		position: fixed;
		width: 100%;
		bottom: 0;
	}
`;

export default ChatbotTemplateContainer;
